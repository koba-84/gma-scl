## 1. W&B alias backfill

- [x] 1.1 `contrastive.model.loss_name` の欠損 run を backfill し、logging/backfill 契約を回帰テストで固定する

## 2. Contrastive metric axis cleanup

- [x] 2.1 contrastive loss metric の `define_metric` を重複しない形へ整理し、W&B metric 定義テストで固定する
