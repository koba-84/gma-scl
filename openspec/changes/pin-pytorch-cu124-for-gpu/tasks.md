## 1. GPU-compatible PyTorch source pin

- [x] 1.1 Pin torch and torchvision to a driver-compatible PyTorch CUDA wheel index, regenerate the lockfile, and verify GPU smoke plus required quality gates.
