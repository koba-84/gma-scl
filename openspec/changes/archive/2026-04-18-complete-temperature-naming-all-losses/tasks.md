## 1. Canonical temperature naming regression guard

- [x] 1.1 Add a config-vs-runtime signature test that covers every supported contrastive loss and rejects legacy temperature keys (`temp`, `tau`, `tau_s`, `temp_attract`, `temp_repulse`), then run required quality checks.
