## Overview

公開 CLI は維持しつつ、rank 境界が未指定のときだけ `[6, 22, 38]` を返す。AAPD は 54 ラベルなので、既定帯は `1-6`, `7-22`, `23-38`, `39-54` になる。

## Decisions

### Decision 1: default はラベル数依存の四分位ではなく固定値にする

- ユーザーが見たい帯は明示されており、四分位自動計算は不要である。
- 既定実行の比較条件を毎回同じに保てる。
