## MODIFIED Requirements

### Requirement: Canonical evaluation project name

This requirement MUST use multi-label-supcon as the wandb project name referenced by evaluation spec.

#### Scenario: Evaluation spec references wandb project

- WHEN 開発者が evaluation main spec の wandb 項目を参照する
- THEN プロジェクト名は `multi-label-supcon` である
