## 1. OpenSpec Contracts

- [x] 1.1 proposal と delta spec で MXCLR agg selector の単一化契約を定義する

## 2. Implementation

- [x] 2.1 MXCLR と agg config を更新し、`agg` を唯一の selector にして `graph_builder` の重複値を削除する

## 3. Verification and Spec Sync

- [x] 3.1 関連 pytest と設定解決確認を通し、main spec へ同期して change を archive する
