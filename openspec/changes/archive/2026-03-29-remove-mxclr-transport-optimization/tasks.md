## 1. OpenSpec Contracts

- [x] 1.1 proposal と delta spec で MXCLR transport backend 固定契約を定義する

## 2. Implementation

- [x] 2.1 MXCLR と transport helper から `transport_optimization` を削除し、transport 系 agg config を同期する

## 3. Verification and Spec Sync

- [x] 3.1 関連 pytest と設定解決確認を通し、main spec へ同期して change を archive する
