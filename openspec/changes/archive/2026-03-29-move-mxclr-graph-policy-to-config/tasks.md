## 1. OpenSpec Contracts

- [x] 1.1 proposal と delta spec で MXCLR graph policy の config 管理契約を定義する

## 2. Implementation

- [x] 2.1 agg config group が concrete graph 実装を直接 instantiate する形へ更新し、MXCLR の総称 builder と selector helper を削除する

## 3. Verification and Spec Sync

- [x] 3.1 関連 pytest と設定解決確認を通し、main spec へ同期して change を archive する
